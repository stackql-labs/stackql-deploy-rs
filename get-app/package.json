{
  "name": "get-stackql-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker behind get-stackql-deploy.io that redirects callers to the right stackql-deploy release asset.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.20.0"
  }
}
